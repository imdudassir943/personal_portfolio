# Django
SECRET_KEY=change-me-to-a-real-secret-key
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.development

# Database (production only — leave empty for SQLite in dev)
# DATABASE_URL=postgres://user:password@host:5432/portfolio_db

# Allowed Hosts (production only)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# CORS (production only)
# CORS_ALLOWED_ORIGINS=https://your-portfolio.vercel.app

# Cloudinary (production only)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret
